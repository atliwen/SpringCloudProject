spring:
  application:
    name: client_OrderBServer
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: edi_thoth_config
eureka:
  client:
    serviceUrl:
      defaultZone: http://edithothserver:9001/eureka/
  instance:
    lease-renewal-interval-in-seconds: 1 
info:
  contact:
    appName: client_OrderBServer
    author: atliwen


