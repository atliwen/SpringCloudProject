spring:
  application:
    name: edi_thoth_webApi
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: edi_thoth_config
info:
  contact:
    appName: edi_thoth_webApi
    author: atliwen
eureka:
  instance:
    hostname: edithothwebApi
    lease-renewal-interval-in-seconds: 1
  client:
    serviceUrl:
      defaultZone: http://edithothserver:9001/eureka/
        
