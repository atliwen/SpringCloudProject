spring:
  application:
    name: edi_thoth_zull
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: edi_thoth_config

eureka:
  client:
    serviceUrl:
      defaultZone: http://edithothserver:9001/eureka/
  instance:
    lease-renewal-interval-in-seconds: 1 

server:
  port: 9007

info:
  contact:
    appName: edi_thoth_zull
    author: atliwen