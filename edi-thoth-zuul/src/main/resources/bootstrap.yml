spring:
  application:
    name: edi_thoth_zull
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: edi_thoth_config

eureka:
  client:
    serviceUrl:
      defaultZone: http://edithothserver:9001/eureka/
  instance:
    hostname: edithothzull

info:
  contact:
    appName: edi_thoth_zull
    author: atliwen
